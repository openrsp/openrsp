#!/usr/bin/env python

import os
import sys

sys.path.append(os.path.join(os.path.dirname(__file__), '..'))
from beholder import Filter
from beholder_openrsp import OpenRSPTestRun

test_run = OpenRSPTestRun(__file__, sys.argv)

f_out = Filter()
f_out.add(from_string = 'Final results from SIRIUS',
          num_lines   = 11,
          tolerance   = 1.0e-7)

f_stdout = Filter()
f_stdout.add(from_string  = 'gamma = -Effff',
             num_lines    = 29,
             tolerance    = 1.0e-7,
             ignore_below = 1.0e-7)

test_run.execute(['gamma.dal'], ['h2o2.mol'], {'out': f_out, 'stdout': f_stdout})

sys.exit(0)
