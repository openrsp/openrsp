#!/usr/bin/env python

import os
import sys

sys.path.append(os.path.join(os.path.dirname(__file__), '..'))
from beholder import Filter
from beholder_openrsp import OpenRSPTestRun

test_run = OpenRSPTestRun(__file__, sys.argv)

f = Filter()
f.add(tolerance = 1.0e-4) # all lines

test_run.execute(['pe_delta1.dal','pe_delta2.dal','pe_delta3.dal'], ['sto_3g.mol'],
                 {'rsp_tensor_human': f}, args='-pot 2_h2o.pot -get rsp_tensor_human')

sys.exit(0)
